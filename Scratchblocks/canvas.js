function render(elem){
  var code = elem.innerHTML.split(' ');
  var c = document.createElement('canvas');
  var ctx = c.getContext('2d');
  elem.appendChild(c);
  var x=0;
  var y=0;
  var size=10;
  var intent=0;
  var start=1;
  var loopcol = [];
  var looph = [];
  function color(col){
    switch(col){
	  case mo:ctx.strokeStyle = '#4a6cd4';break;
	  case 
	}
  }
}